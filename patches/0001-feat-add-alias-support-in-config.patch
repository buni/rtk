From e14d27c1d92952d3263b45642125c33e903b3870 Mon Sep 17 00:00:00 2001
From: Hristo <xkyrchokov@gmail.com>
Date: Fri, 20 Mar 2026 12:23:40 +0000
Subject: [PATCH] feat: add alias support in config

---
 src/cmds/python/pytest_cmd.rs |  2 +-
 src/cmds/rust/cargo_cmd.rs    |  2 +-
 src/core/config.rs            | 20 +++++++++
 src/hooks/rewrite_cmd.rs      | 30 +++++++++++--
 src/main.rs                   | 82 +++++++++++++++++++++++++++++++++++
 5 files changed, 130 insertions(+), 6 deletions(-)

diff --git a/src/cmds/python/pytest_cmd.rs b/src/cmds/python/pytest_cmd.rs
index cd27943..38c09e6 100644
--- a/src/cmds/python/pytest_cmd.rs
+++ b/src/cmds/python/pytest_cmd.rs
@@ -49,7 +49,7 @@ pub fn run(args: &[String], verbose: u8) -> Result<i32> {
 }
 
 /// Parse pytest output using state machine
-fn filter_pytest_output(output: &str) -> String {
+pub fn filter_pytest_output(output: &str) -> String {
     let mut state = ParseState::Header;
     let mut test_files: Vec<String> = Vec::new();
     let mut failures: Vec<String> = Vec::new();
diff --git a/src/cmds/rust/cargo_cmd.rs b/src/cmds/rust/cargo_cmd.rs
index 9d6b385..210ea5c 100644
--- a/src/cmds/rust/cargo_cmd.rs
+++ b/src/cmds/rust/cargo_cmd.rs
@@ -732,7 +732,7 @@ impl AggregatedTestResult {
 }
 
 /// Filter cargo test output - show failures + summary only
-fn filter_cargo_test(output: &str) -> String {
+pub fn filter_cargo_test(output: &str) -> String {
     let mut failures: Vec<String> = Vec::new();
     let mut summary_lines: Vec<String> = Vec::new();
     let mut in_failure_section = false;
diff --git a/src/core/config.rs b/src/core/config.rs
index 8d3da72..cf5b633 100644
--- a/src/core/config.rs
+++ b/src/core/config.rs
@@ -21,6 +21,26 @@ pub struct Config {
     pub hooks: HooksConfig,
     #[serde(default)]
     pub limits: LimitsConfig,
+    #[serde(default)]
+    pub aliases: AliasesConfig,
+}
+
+/// Command aliases: map a command prefix to an existing RTK filter name.
+///
+/// When RTK sees a command matching an alias prefix, it runs the original
+/// command unchanged (preserving virtualenvs, make targets, etc.) but filters
+/// the output using the named RTK filter.
+///
+/// Example in `~/.config/rtk/config.toml`:
+/// ```toml
+/// [aliases]
+/// "uv run pytest" = "pytest"
+/// "make test"     = "pytest"
+/// ```
+#[derive(Debug, Serialize, Deserialize, Default, Clone)]
+pub struct AliasesConfig {
+    #[serde(flatten)]
+    pub map: std::collections::HashMap<String, String>,
 }
 
 #[derive(Debug, Serialize, Deserialize, Default)]
diff --git a/src/hooks/rewrite_cmd.rs b/src/hooks/rewrite_cmd.rs
index 172d758..fb3d667 100644
--- a/src/hooks/rewrite_cmd.rs
+++ b/src/hooks/rewrite_cmd.rs
@@ -16,9 +16,8 @@ use std::io::Write;
 /// | 2    | (none)   | Deny rule matched — hook defers to Claude Code native deny.  |
 /// | 3    | rewritten| Ask rule matched — hook rewrites but lets Claude Code prompt.|
 pub fn run(cmd: &str) -> anyhow::Result<()> {
-    let excluded = crate::core::config::Config::load()
-        .map(|c| c.hooks.exclude_commands)
-        .unwrap_or_default();
+    let config = crate::core::config::Config::load().unwrap_or_default();
+    let excluded = config.hooks.exclude_commands.clone();
 
     // SECURITY: check deny/ask BEFORE rewrite so non-RTK commands are also covered.
     let verdict = check_command(cmd);
@@ -27,7 +26,30 @@ pub fn run(cmd: &str) -> anyhow::Result<()> {
         std::process::exit(2);
     }
 
-    match registry::rewrite_command(cmd, &excluded) {
+    // Alias check: if cmd matches a user alias prefix, rewrite to `rtk <cmd>`
+    // so run_fallback in main.rs executes it and applies the named filter.
+    // Routed through the same verdict match as registry rewrites, so aliases
+    // never bypass permission checks.
+    let alias_rewritten: Option<String> = {
+        let trimmed = cmd.trim();
+        if !trimmed.starts_with("rtk ") {
+            let any_match = config.aliases.map.keys().any(|prefix| {
+                let p = prefix.as_str();
+                trimmed == p || trimmed.starts_with(&format!("{} ", p))
+            });
+            if any_match {
+                Some(format!("rtk {}", trimmed))
+            } else {
+                None
+            }
+        } else {
+            None
+        }
+    };
+
+    let resolved = alias_rewritten.or_else(|| registry::rewrite_command(cmd, &excluded));
+
+    match resolved {
         Some(rewritten) => match verdict {
             PermissionVerdict::Allow => {
                 print!("{}", rewritten);
diff --git a/src/main.rs b/src/main.rs
index f5a7e9d..042b78c 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -1091,6 +1091,56 @@ fn run_fallback(parse_error: clap::Error) -> Result<i32> {
             .collect::<Vec<_>>()
             .join(" ")
     };
+
+    // Config alias lookup — runs the original command (preserving venv/make
+    // targets) but filters output through a named RTK filter.
+    let alias_filter_name = {
+        let aliases = core::config::Config::load()
+            .map(|c| c.aliases.map)
+            .unwrap_or_default();
+        find_alias_match(&lookup_cmd, &aliases)
+    };
+
+    if let Some(ref filter_name) = alias_filter_name {
+        let result = core::utils::resolved_command(&args[0])
+            .args(&args[1..])
+            .stdin(std::process::Stdio::inherit())
+            .stdout(std::process::Stdio::piped())
+            .stderr(std::process::Stdio::inherit())
+            .output();
+
+        return match result {
+            Ok(output) => {
+                let exit_code = core::utils::exit_code_from_output(&output, &raw_command);
+                let stdout_raw = String::from_utf8_lossy(&output.stdout);
+                let tee_hint = if !output.status.success() {
+                    core::tee::tee_and_hint(&stdout_raw, &raw_command, exit_code)
+                } else {
+                    None
+                };
+                let filtered = apply_named_filter(filter_name, &stdout_raw)
+                    .unwrap_or_else(|| stdout_raw.to_string());
+                println!("{}", filtered);
+                if let Some(hint) = tee_hint {
+                    println!("{}", hint);
+                }
+                timer.track(
+                    &raw_command,
+                    &format!("rtk:alias({}) {}", filter_name, raw_command),
+                    &stdout_raw,
+                    &filtered,
+                );
+                core::tracking::record_parse_failure_silent(&raw_command, &error_message, true);
+                Ok(exit_code)
+            }
+            Err(e) => {
+                core::tracking::record_parse_failure_silent(&raw_command, &error_message, false);
+                eprintln!("[rtk: {}]", e);
+                Ok(127)
+            }
+        };
+    }
+
     let toml_match = if std::env::var("RTK_NO_TOML").ok().as_deref() == Some("1") {
         None
     } else {
@@ -1223,6 +1273,38 @@ enum GtCommands {
     Other(Vec<OsString>),
 }
 
+/// Find the longest alias prefix that matches `cmd`.
+/// Returns the target filter name (e.g. "pytest") or None.
+fn find_alias_match(
+    cmd: &str,
+    aliases: &std::collections::HashMap<String, String>,
+) -> Option<String> {
+    let mut best: Option<(usize, String)> = None;
+    for (prefix, target) in aliases {
+        let matches = cmd == prefix.as_str() || cmd.starts_with(&format!("{} ", prefix));
+        if matches {
+            let len = prefix.len();
+            if best.as_ref().is_none_or(|(bl, _)| len > *bl) {
+                best = Some((len, target.clone()));
+            }
+        }
+    }
+    best.map(|(_, t)| t)
+}
+
+/// Apply a named RTK filter function to captured stdout.
+/// Returns None if the filter name is unknown (caller falls back to raw output).
+fn apply_named_filter(name: &str, stdout: &str) -> Option<String> {
+    match name {
+        "pytest" => Some(cmds::python::pytest_cmd::filter_pytest_output(stdout)),
+        "mypy" => Some(cmds::python::mypy_cmd::filter_mypy_output(stdout)),
+        "ruff check" | "ruff" => Some(cmds::python::ruff_cmd::filter_ruff_check_json(stdout)),
+        "ruff format" => Some(cmds::python::ruff_cmd::filter_ruff_format(stdout)),
+        "cargo test" => Some(cmds::rust::cargo_cmd::filter_cargo_test(stdout)),
+        _ => None,
+    }
+}
+
 /// Split a string into shell-like tokens, respecting single and double quotes.
 /// e.g. `git log --format="%H %s"` → ["git", "log", "--format=%H %s"]
 fn shell_split(input: &str) -> Vec<String> {
-- 
2.43.0

